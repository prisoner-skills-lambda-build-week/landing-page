{
  "name": "p-skills",
  "version": "1.0.0",
  "description": "Lambda build week project",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "eslint:init": "eslint --init",
    "sass": "sass --watch src/styles:src/assets/css",
    "sassOnce": "sass src/styles:src/assets/css",
    "clean": "gulp clean",
    "build": "gulp",
    "watch": "gulp watch",
    "dev": "yarn sass && yarn watch",
    "deploy": "yarn sassOnce && yarn build && firebase deploy"
  },
  "author": "melissa murphy",
  "license": "ISC",
  "devDependencies": {
    "bulma": "^0.7.4",
    "eslint": "^5.16.0",
    "eslint-config-google": "^0.12.0",
    "prettier": "^1.17.0",
    "prettier-stylelint": "^0.4.2"
  },
  "dependencies": {
    "del": "^4.1.0",
    "eslint-plugin-jsdoc": "^4.8.3",
    "gulp": "^4.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-plumber": "^1.2.1",
    "stylelint": "^10.0.1",
    "stylelint-config-idiomatic-order": "^6.2.0",
    "stylelint-config-standard": "^18.3.0"
  }
}
